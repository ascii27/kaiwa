{
  "name": "kaiwa",
  "private": true,
  "packageManager": "pnpm@8.10.0",
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "format": "pnpm -r format",
    "prepare": "husky install"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.1.1"
  }
}
